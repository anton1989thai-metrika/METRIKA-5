# Database
DATABASE_URL="file:./prisma/dev.db"

# Email Configuration - IMAP (для получения писем)
IMAP_HOST="mail.metrika.direct"
IMAP_PORT="993"
IMAP_USER="info@metrika.direct"
IMAP_PASS="ваш_пароль_от_почтового_ящика"

# Email Configuration - SMTP (для отправки писем)
SMTP_HOST="mail.metrika.direct"
SMTP_PORT="587"
SMTP_USER="info@metrika.direct"
SMTP_PASS="ваш_пароль_от_почтового_ящика"
SMTP_FROM="info@metrika.direct"

# Список ящиков для синхронизации (опционально)
# Если не указано, будут синхронизироваться все ящики:
# derik, savluk, ionin, manager, smm, info, reg, kadastr, lawyer, kan
# SYNC_EMAILS="derik@metrika.direct,savluk@metrika.direct,info@metrika.direct"

# Next.js
NEXT_PUBLIC_APP_URL="http://localhost:3000"

