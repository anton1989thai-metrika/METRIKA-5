# 🔄 Автоматическая синхронизация с GitHub

Эта система автоматически синхронизирует изменения между вашим локальным проектом и GitHub **в обе стороны**:

- ✅ **Локальные изменения → GitHub** (автоматически при коммите)
- ✅ **Изменения на GitHub → локально** (при запуске синхронизации)

## 🚀 Быстрый старт

### Вариант 1: Автоматическая синхронизация при коммитах (уже настроено)

**Что происходит:**
- Каждый раз, когда вы делаете `git commit`, изменения **автоматически** отправляются на GitHub
- Ничего дополнительного делать не нужно!

**Пример:**
```bash
# Изменяете файл
nano src/app/page.tsx

# Добавляете и коммитите
git add .
git commit -m "Обновил главную страницу"

# ✅ Изменения АВТОМАТИЧЕСКИ отправляются на GitHub!
```

### Вариант 2: Ручная синхронизация в обе стороны

**Однократная синхронизация:**
```bash
npm run auto-sync
# или
./auto-sync.sh
```

Этот скрипт:
1. 📥 Получает изменения с GitHub (pull)
2. 📤 Отправляет ваши локальные изменения на GitHub (push)

### Вариант 3: Постоянное отслеживание изменений (рекомендуется)

**Запуск в фоновом режиме:**
```bash
npm run watch-sync
# или
./watch-sync.sh
```

Этот скрипт:
- 👀 Следит за изменениями файлов
- 🔄 Автоматически синхронизирует при любых изменениях
- ⏹️ Остановить: нажмите `Ctrl+C`

**Для лучшей производительности на macOS:**
```bash
brew install fswatch
```

## 📋 Как это работает

### Автоматический push (уже настроено)

Файл `.git/hooks/post-commit` автоматически запускается после каждого коммита и отправляет изменения на GitHub.

### Ручная синхронизация

Скрипт `auto-sync.sh`:
1. Проверяет изменения на GitHub
2. Загружает их локально (с сохранением ваших незакоммиченных изменений)
3. Отправляет ваши локальные изменения на GitHub

### Отслеживание изменений

Скрипт `watch-sync.sh`:
- Использует `fswatch` (macOS) или `inotifywait` (Linux) для отслеживания файлов
- При изменении файла автоматически запускает синхронизацию
- Fallback: проверяет изменения каждые 30 секунд

## ⚙️ Настройки

### Отключить автоматический push

Если хотите отключить автоматический push после коммита:

```bash
chmod -x .git/hooks/post-commit
```

Включить обратно:
```bash
chmod +x .git/hooks/post-commit
```

### Изменить ветку

По умолчанию используется ветка `main`. Чтобы изменить:

1. Откройте `auto-sync.sh`
2. Найдите `git pull origin main`
3. Замените `main` на вашу ветку

## 🔍 Проверка статуса

Посмотреть статус синхронизации:
```bash
git status
git fetch
git log HEAD..origin/main  # Изменения на GitHub, которых нет локально
git log origin/main..HEAD  # Ваши локальные изменения, которых нет на GitHub
```

## ⚠️ Важные моменты

1. **Конфликты:** Если на GitHub и локально изменены одни и те же файлы, Git попросит разрешить конфликт вручную
2. **Незакоммиченные изменения:** При pull незакоммиченные изменения сохраняются через `git stash`
3. **Безопасность:** Система не удаляет файлы автоматически - все операции можно отследить через `git log`

## 📝 Примеры использования

### Ежедневная работа

```bash
# Запускаете watch-sync утром
npm run watch-sync

# Работаете над проектом - все автоматически синхронизируется
# Изменения на GitHub → локально
# Ваши изменения → GitHub
```

### Быстрая синхронизация

```bash
# Просто запускаете один раз
npm run auto-sync
```

### Ручная отправка с описанием

```bash
# Для важных изменений используйте обычный процесс
git add .
git commit -m "Добавлена новая функция"
# Push произойдет автоматически!
```

## 🆘 Решение проблем

**Проблема:** Конфликты при синхронизации
```bash
# Разрешите конфликт вручную, затем:
git add .
git commit -m "Разрешен конфликт"
```

**Проблема:** Watch-sync не работает
```bash
# Установите fswatch для macOS
brew install fswatch
```

**Проблема:** Не отправляются изменения на GitHub
```bash
# Проверьте права доступа
git remote -v
# Проверьте, что вы авторизованы
git push origin main
```

---

**Готово!** Теперь ваши изменения автоматически синхронизируются с GitHub! 🎉

